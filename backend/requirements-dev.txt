# Development and test dependencies.
# Deliberately separate from requirements.txt: the production image installs
# only runtime dependencies, so the test runner never ships to deployment.
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.5
