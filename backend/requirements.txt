fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.1.0
aiosqlite>=0.19.0
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.1
pgvector>=0.2.4
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6
httpx>=0.26.0
python-dotenv>=1.0.0
PyPDF2>=3.0.1
python-docx>=1.1.0
pytest>=8.0.0
pytest-asyncio>=0.23.5
redis>=5.0.1
beautifulsoup4>=4.12.0

# ── New dependencies for 4-module upgrade ────────────────────────────────────
websockets>=12.0             # Gemini Live API WebSocket client (Module 3)
deepgram-sdk>=3.5.0         # Streaming STT via Deepgram Nova-2 (Module 3B)
numpy>=1.26.0               # TF-IDF cosine similarity for taxonomy matching (Module 1)

# ── Voice runtime dependencies (Kokoro TTS / Whisper STT active providers) ───
# Imported at module load by app/providers/kokoro_tts.py and whisper_stt.py:
torch>=2.13.0                # Kokoro synthesis tensors (top-level import)
soundfile>=0.14.0            # WAV encode/decode in both voice providers
# Imported when the active provider initialises its pipeline:
transformers>=5.16.1         # Whisper ASR pipeline
kokoro>=0.9.4                # Kokoro KPipeline (version per test_kokoro_whisper_integration.py)
# Audio decode/resample path for browser-recorded audio:
librosa>=1.0.0               # WebM/MP3 decode + resample to 16 kHz
scipy>=1.18.1                # Resampling fallback in the Whisper decode path
